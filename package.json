{
  "name": "rag-project",
  "version": "1.0.0",
  "description": "RAG (Retrieval Augmented Generation) Full Stack Application",
  "private": true,
  "scripts": {
    "dev": "npm-run-all --parallel dev:backend dev:frontend",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd RDA-frontend && npm run dev",
    "build": "npm run build --workspaces --if-present"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  },
  "dependencies": {
    "@prisma/client": "^7.8.0",
    "prisma": "^6.19.3"
  }
}
